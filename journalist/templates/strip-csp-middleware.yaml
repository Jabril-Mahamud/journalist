{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-csp
spec:
  headers:
    customResponseHeaders:
      Content-Security-Policy: ""
{{- end }}
